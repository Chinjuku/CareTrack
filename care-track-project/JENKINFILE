pipeline {
    agent any
    environment {
        DOCKER_IMAGE       = 'Ionelyxd/project-app:latest' 
        DOCKER_CREDENTIALS = credentials('dockerhub')
    }

    stages {
        stage('Start Jenkins') {
            steps {
                script {
                    node {
                        sh 'echo Start Jenkins............'
                        sh 'echo docker : user = $DOCKER_CREDENTIALS_USR : password = $DOCKER_CREDENTIALS_PSW'
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    node {
                        dir('./') { 
                            sh 'echo "Running in $(pwd)"'
                            sh 'echo start building the Docker image = $DOCKER_IMAGE'
                            sh 'docker build -t $DOCKER_IMAGE .'
                        }
                    }
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    node {
                        sh 'echo $DOCKER_CREDENTIALS_PSW | docker login --username $DOCKER_CREDENTIALS_USR --password-stdin'
                        sh 'docker push $DOCKER_IMAGE'
                    }
                }
            }
        }

        stage('Clear Docker Components') {
            steps {
                script {
                    node {
                        sh 'docker stop $(docker ps -a -q) || true'  
                        sh 'docker rm $(docker ps -a -q) || true' 
                        sh 'docker rmi $(docker images -q) || true'
                        sh 'docker system prune -af'
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    node {
                        sh 'docker pull $DOCKER_IMAGE'
                        sh 'docker run -d --name project-app -p 8085:80 $DOCKER_IMAGE'
                    }
                }
            }
        }
    }

    post {
        always {
            script {
                node {
                    sh 'docker logout'
                }
            }
        }
    }
}
